<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Blazor AP1 P2 </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
    

            <div class="nav-item px-3">
                <NavLink class="nav-link" href="RegistroProductos">
                    <span class="oi oi-plus" aria-hidden="true"></span> Registro Productos
                </NavLink>
            </div>

                <div class="nav-item px-3">
            <NavLink class="nav-link" href="ConsultaProductos">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Consulta Productos
            </NavLink>
        </div> 

         <div class="nav-item px-3">
            <NavLink class="nav-link" href="ConsultaDetalles">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Consulta de Detalles
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="EntradaProductosEmpacados">
                <span class="oi oi-plus" aria-hidden="true"></span> Entrada de Empacados
            </NavLink>
        </div>
       
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
